import { writable } from 'svelte/store';

export const recensioni = writable([]);

export const piattiRistorante = writable([]);

export const idPiatto = writable(0);

export const objPiatto = writable([]);
